import 'package:flutter/material.dart';

abstract class AppRadius {
  static const Radius circular10 = Radius.circular(10);
  static const Radius circular15 = Radius.circular(15);
  static const Radius circular25 = Radius.circular(25);
  static const Radius circular50 = Radius.circular(50);
}
