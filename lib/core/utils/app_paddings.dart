import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets all8 = EdgeInsets.all(8.0);
  static const EdgeInsets all30 = EdgeInsets.all(30.0);
  static const EdgeInsets vertical8 = EdgeInsets.symmetric(vertical: 8);
  static const EdgeInsets horizontal8 = EdgeInsets.symmetric(horizontal: 8);

  ///buttons
  static const nextButton = EdgeInsets.only(
    left: 15,
    top: 5,
    right: 25,
    bottom: 5,
  );
  static const skipButton = EdgeInsets.only(
    left: 25,
    top: 5,
    right: 15,
    bottom: 5,
  );
}
